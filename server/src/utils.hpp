#include <string>
#include <vector>

using namespace std;

/**
 * Splits provided string into a vector of arguments.
 *
 * @param str - String to split.
 *
 * @returns string vector
 */
vector<string> split(string str);